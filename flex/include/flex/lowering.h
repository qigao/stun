/*
 * Flex Engine - Lowering Module
 *
 * Semantic entry for converting parsed DSL structures into executable runtime
 * objects. This replaces the older "bridge" wording for AST/runtime lowering.
 */

#pragma once

#include "flex/lowering/ast_to_runtime.h"
