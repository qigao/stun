/*
 * Flex Engine - Type Implementations
 *
 * Minimal implementation - most functions are inline in types.h
 */

#include "flex/types.h"
#include <cstring>
#include <cstdlib>

namespace flex {

// Simple implementations that need separate .cpp file
// Most are inline in types.h for performance

} // namespace flex
