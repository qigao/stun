See README.md in the parent directory.

Local changes
-------------

2026-07-15: `libproject/project.cpp` replaces standard-library function
adapters removed by C++17 (`mem_fun`, `bind2nd`, and `unary_function`) with
equivalent lambdas/functors. The solver algorithm and public ABI are unchanged.
