Material animation assets
============================

Drop your `.json` Lottie files in this directory. The Android demo looks for
`material_spinner.json`; if rlottie is enabled and that file exists it will be
played inside the detail screen. Any compatible Lottie animation can be used.
