From: Stun maintainers
Subject: [PATCH] rtasm: release Linux module-owned executable arena at unload

Downstream patch for Mesa 25.2.8; tracks qigao/stun#31 and #42.
Exact lifecycle evidence attributes the remaining Mesa-owned leak only to the
rtasm executable arena. Keep it shared while contexts can use the module, then
release it at module unload after ordinary finalizers. No public cleanup entry
point, suppression, CPU-topology mutation, or per-context teardown is added.

diff --git a/src/gallium/auxiliary/rtasm/rtasm_execmem.c b/src/gallium/auxiliary/rtasm/rtasm_execmem.c
--- a/src/gallium/auxiliary/rtasm/rtasm_execmem.c
+++ b/src/gallium/auxiliary/rtasm/rtasm_execmem.c
@@ -66,6 +66,25 @@ static struct mem_block *exec_heap = NULL;
 static unsigned char *exec_mem = NULL;
 
 
+#if DETECT_OS_LINUX
+/* This cache belongs to the module, not to a single context. Run after normal
+ * finalizers/queue shutdown, while the allocator's mutex is still available.
+ */
+static void __attribute__((destructor(101)))
+rtasm_execmem_module_cleanup(void)
+{
+   simple_mtx_lock(&exec_mutex);
+   u_mmDestroy(exec_heap);
+   exec_heap = NULL;
+
+   if (exec_mem && exec_mem != MAP_FAILED)
+      munmap(exec_mem, EXEC_HEAP_SIZE);
+   exec_mem = NULL;
+   simple_mtx_unlock(&exec_mutex);
+}
+#endif
+
+
 static int
 init_heap(void)
 {
