# Project Overview

This repository contains a C/C++ UI + charting stack (Flex + chart engines).

## Charts

Chart engines are under `charts/`. Mermaid chart engines live here:

- `charts/mermaid/README.md`

